import {BaseTimeline} from './base-timeline';

export class ExperienceTimeline extends BaseTimeline {
  companyName: string;
  dateFrom: Date;
  dateTo: Date;
  jobPosition: string;
  comment: string;
}
